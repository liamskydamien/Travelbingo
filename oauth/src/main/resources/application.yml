server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/travelbingo
    username: travelbingoadmin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
